require 'spec_helper'

describe Squareone do
  it 'has a version number' do
    expect(Squareone::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
